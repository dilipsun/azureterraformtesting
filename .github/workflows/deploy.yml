name: Deploy

on:
  push:
    branches: [main]

jobs:
  terraform:
    uses: dilipsun/oidc-auth-workflow/.github/workflows/terraform-deploy.yml@main
    permissions:
      id-token: write
      contents: read
